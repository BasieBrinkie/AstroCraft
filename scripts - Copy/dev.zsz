#priority 100000

/*
	-------------------------------------------------------------------------
	Some dev settings, only change these if you are testing or doing packdev.
	-------------------------------------------------------------------------
*/

global packVersion as string = "0.0.1";

global dev as bool = true;
global devStages as bool = true; //This setting might up the loading time drastically